 # ==========================================
# PROJEKT: WELLUMINÖSER AGB-SCANNER
# VERSION: 0.2 - "Der Risiko-Check"
# ==========================================

def agb_check_pro(text):
    # Gefahren mit Risiko-Punkten (1-10)
    gefahren = {
        "datenweitergabe": (8, "Deine privaten Daten werden verkauft."),
        "abo": (9, "Mögliche Kostenfalle durch automatische Verlängerung."),
        "drittanbieter": (7, "Zugriff durch externe Firmen."),
        "werbezwecke": (4, "Deine Daten werden für Werbung genutzt."),
        "kündigungsfrist": (6, "Lange Bindung möglich – prüfe die Fristen."),
        "schadenersatz": (5, "Komplizierte Haftungsregeln gefunden.")
    }
    
    gesamt_risiko = 0
    gefundene_treffer = 0
    text = text.lower()
    
    print("--- 🔍 WELLUMINÖSER SCAN STARTET ---")
    
    for wort, (punkte, info) in gefahren.items():
        if wort in text:
            print(f"⚠️ [{punkte}/10] {wort.upper()}: {info}")
            gesamt_risiko += punkte
            gefundene_treffer += 1
            
    print("-" * 30)
    
    if gefundene_treffer == 0:
        print("✅ ERGEBNIS: Alles im grünen Bereich. Viel Spaß!")
    else:
        durchschnitt = gesamt_risiko / gefundene_treffer
        print(f"📊 RISIKO-SCORE: {durchschnitt:.1f} / 10")
        
        if durchschnitt >= 7:
            print("🚨 FAZIT: Sehr riskant! Lies das auf jeden Fall nochmal genau.")
        elif durchschnitt >= 4:
            print("🟡 FAZIT: Ein paar Haken sind dabei. Sei wachsam.")
        else:
            print("🔵 FAZIT: Sieht weitgehend fair aus.")

# Hier zum Testen einen Text einfügen
beispiel = "Dieses Abo beinhaltet die Datenweitergabe an Drittanbieter für Werbezwecke."
agb_check_pro(beispiel)
